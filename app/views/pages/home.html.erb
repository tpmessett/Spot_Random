<div class="container">
  <h1>Pages#home</h1>
  <p><%= current_user.uid %></p>
  <% @me.playlists(limit: 50, offset: 0).each_with_index do |playlist, index| %>
    <p><%=index + 1%>) <%=playlist.name%></p>
  <% end %>
</div>
